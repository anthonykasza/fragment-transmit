package main
import "fmt"

func main() {
  var thing1 int
  thing2 := int(0)
  var thing3 int = 0

  // all those things do the same thing... phew, I'm exhausted
  fmt.Println(thing1, thing2, thing3)
}
